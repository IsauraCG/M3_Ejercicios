""" MODULOS
se importan como librerías al proyecto, y realizan alguna acción """
from math import pi as PI_VALUE #importa 'pi' desde modulo 'math' con el alias'PI_VALUE'
from math import * #importa todos los elementos de la librería 'math'
import math #importa la librería 'math' completa

print(math.sqrt(2)) #imprime el cálculo de la raiz cuadrada de 2
print(math.exp(2))  #imprime el exponente de 2
print(math.pow(2,3))#imprime 8.0